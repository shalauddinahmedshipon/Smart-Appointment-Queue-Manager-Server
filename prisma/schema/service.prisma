model Service {
  id                String   @id @default(uuid())
  name              String
  duration          Int      // 15 / 30 / 60 minutes
  requiredStaffType String

  accountId         String
  account           Account  @relation(fields: [accountId], references: [id])

  appointments      Appointment[]
}
